[project]
name = "pylopdf-cloudflare-worker"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["pylopdf>=0.11,<0.12"]

[dependency-groups]
dev = [
    "workers-py>=1.15,<2",
    "workers-runtime-sdk",
]
